在链表中找到一个节点并移除它，实现alloc方法的中心操作
```rust
// in src/allocator/linked_list.rs

impl LinkedListAllocator {
    /// 查找给定大小和对齐方式的空闲区域并将其从链表中移除。
    ///
    /// 返回一个包含链表节点和分配内存区域起始地址的元组。
    fn find_region(&mut self, size: usize, align: usize)
        -> Option<(&'static mut ListNode, usize)>
    {
        // 当前链表节点的引用，每次迭代更新
        let mut current = &mut self.head;
        // 在链表中查找合适大小的内存区域
        while let Some(ref mut region) = current.next {
            if let Ok(alloc_start) = Self::alloc_from_region(&region, size, align) {
                // 区域适用于分配 -> 从链表中移除该节点
                let next = region.next.take();
                let ret = Some((current.next.take().unwrap(), alloc_start));
                current.next = next;
                return ret;
            } else {
                // 区域不适用 -> 继续下一个区域
                current = current.next.as_mut().unwrap();
            }
        }
        // 未找到合适的区域
        None
    }
}
```
### 核心梳理
- https://os.phil-opp.com/zh-CN/allocator-designs/#shi-xian-1

### 知识点
- [Rust-访问一个字段的方法时，隐式可变借用父结构体](../../../../../../Zettelkasten/permanent/Rust-访问一个字段的方法时，隐式可变借用父结构体.md)
- [Rust-链表中这种不断向后的引用链的生命周期转换](../../../../../../Zettelkasten/permanent/Rust-链表中这种不断向后的引用链的生命周期转换.md)
- [Rust-模式匹配-不转移所有权](../../../../../../Zettelkasten/permanent/Rust-模式匹配-不转移所有权.md)
- [Rust-模式匹配中的生命周期依赖](../../../../../../Zettelkasten/permanent/Rust-模式匹配中的生命周期依赖.md)