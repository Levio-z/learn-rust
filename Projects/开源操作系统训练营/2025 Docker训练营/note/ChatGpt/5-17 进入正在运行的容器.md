```
➜  /workspace git:(main) docker exec -it bf506ee52fe0 /bin/bash

root@bf506ee52fe0:/# exit
exit
```

进入可以使用查看进程信息
```
ps -ef
```
命令解释：

- `ps`：process status，显示进程状态。
    
- `-e`：显示**所有进程**（等价于 `-A`）。
    
- `-f`：使用**完整格式**显示进程信息（包含 UID、PID、PPID、C、STIME、TTY、TIME、CMD 等字段）。