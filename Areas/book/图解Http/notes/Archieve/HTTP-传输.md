---
tags:
  - note
---
- **含义：** “传输”是指数据在两个实体之间进行移动的过程。
    
- **在 HTTP 中的意义：** HTTP 定义了这种数据移动的**规则和格式**。它规定了客户端（通常是浏览器）如何**请求**资源，以及服务器如何将资源作为**响应**发回。
    
- **核心功能：** HTTP 是一种**请求-响应**协议，是数据通信的桥梁。它负责将 Web 资源（如超文本、图片、样式表、数据）从服务器端**传输**到客户端。