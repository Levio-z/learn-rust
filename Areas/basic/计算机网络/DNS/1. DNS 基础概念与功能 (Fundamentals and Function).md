- **1.1 定义：** 域名系统（Domain Name System）。
- **1.2 核心功能：** 将人**类易记的域名 (Hostname) 转换为机器可识别的 IP 地址**，实现**域名解析**。
- **1.3 协议定位：** 属于 TCP/IP 协议簇的**应用层**协议。
    
    - **传输协议：** 通常使用 **UDP** 协议，端口 **53**（快速查询）；对于大型数据传输（如区域传输）或可靠性要求高的场景，使用 **TCP** 协议，端口 53。
        