- **7.1 DNSSEC (DNS Security Extensions)：**
    
    - 作用：通过数字签名，确保 DNS 数据的**真实性和完整性**，防止 DNS 缓存投毒。
        
- **7.2 DNS over HTTPS (DoH) / DNS over TLS (DoT)：**
    
    - 作用：通过加密通道传输 DNS 查询请求，防止查询内容被 ISP 或中间人监听。
        
- **7.3 反向解析：** 通过 PTR 记录，将 IP 地址解析为域名（用于邮件服务器反垃圾邮件检查）。