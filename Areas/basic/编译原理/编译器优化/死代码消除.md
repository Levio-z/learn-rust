### 1. 编译器优化掉的原因
现代编译器（Rust/LLVM、GCC 等）会进行 **死代码消除（Dead Code Elimination）**：
- **原则**：如果一个操作的结果**没有被程序的可观测行为使用**，编译器就可以安全地删掉它。
- **例子**：
    `let mut x = 0; x = 0; // 编译器可能会认为这行代码对程序行为没有影响，可以优化掉`
- 解释：
    - `x` 是局部变量，它赋值 0 后，程序没有读取它。
    - 对编译器来说，这次写入“没有副作用”，可以省略。