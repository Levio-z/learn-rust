- **2.1 域名的树状结构：** 域名使用句点（`.`）分隔，越靠右层级越高。
- **2.2 顶级结构 (从右到左)：*
    - **根域 (.)：** 树状结构的顶点，通常省略。
    - **顶级域 (TLD)：** `.com`, `.org`, `.cn`, `.net`, `.gov` 等。
    - **二级域：** `google.com`, `server.cn` 中的 `google` 或 `server` 部分。
    - **子域/主机名：** `www.google.com` 中的 `www`。