# 一、理论基础

## 1.1 定义

- 数组是存放在**连续**内存空间上的相同类型数据的集合。
## 1.2 特性

- 获取数据：方便的通过**下标索引的方式**获取到下标下对应的数据
- 因为数组在内存空间的地址是连续的，**删除或者增添元素**的时候，就难免要**移动其他元素的地址**。
> 所以删除对于数组来说其实是覆盖操作
## 1.3 二维数组
[不同编程语言的内存管理是不一样的](https://www.programmercarl.com/%E6%95%B0%E7%BB%84%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)
- 以C++为例，在C++中二维数组是连续分布的。像Java是没有指针的，同时也不对程序员暴露其元素的地址，寻址操作完全交给虚拟机。所以看不到每个元素的地址情况，这里我以Java为例，也做一个实验。 二维数组在内存空间可不是一个 `3*4` 的连续地址空间 所以**Java的二维数组在内存中不是 `3*4` 的连续地址空间，而是四条连续的地址空间组成！**

# 二分法

## 移除元素

- 双指针法（快慢指针法）：**通过一个快指针和慢指针在一个for循环下完成两个for循环的工作。**
    - 暴力解法时间复杂度：O(n^2)
    - 双指针时间复杂度：O(n)
- 纠结于数组中的元素为什么不能删除
    - 数组在内存中是连续的地址空间，不能释放单一元素，如果要释放，就是全释放（程序运行结束，回收内存栈空间）。
    - C++中vector和array的区别一定要弄清楚，vector的底层实现是array，封装后使用更友好。
- 指针法（快慢指针法）在数组和链表的操作中是非常常见的，很多考察数组和链表操作的面试题，都使用双指针法。

### 2 移除元素模板

- 快慢指针，在同一个数组中
    - [**27. 移除元素**](https://www.notion.so/27-1e055e4de824802ea902d0312af43f15?pvs=21)
        - 遍历现有元素
        - 一个指针是保留元素数量也是下一个元素插入位置：设定一个指针为当前数组起始位置，下一个可以插入元素的位置
        - 一个指针遍历元素：不满足条件就将这个元素获取插入进来
    - [**283. 移动零**](https://www.notion.so/283-1e055e4de82480ce8049c113c7304eff?pvs=21)
        - 删除0
        - 后面元素都赋值为0就可以了

## 使用栈来完成退格
[844. 比较含退格的字符串](../../../Area/atomic/LeetCode/Done/844.%20比较含退格的字符串.md)

## 两端双指针：比较两个有序序列完成合并

[977 有序数组平方排序](../../../Area/atomic/LeetCode/Done/977%20有序数组平方排序.md)
- 利用元素已经有序的信息,双指针从两端合并
- 问题简化为找绝对值最大的元素
    - 从两端找

### 滑动窗口：
[209. 长度最小的子数组](../../../Area/atomic/LeetCode/Done/209.%20长度最小的子数组.md)

### 螺旋矩阵

[59.螺旋矩阵](https://www.notion.so/59-20455e4de82480309da7e3a97dadebfd?pvs=21)

- 抽象四种步数
    - const DIRS: `[(i32,i32);4] =[(0,1),(1,0),(0,-1),(-1,0)]`;
        - 要有类型
		- ` let mut vec = vec![vec![0;n as usize];n as usize]`;
    - di = (di + 1) % 4; // 右转 90°
- 条件
    - 出界
    - 已经有值

### 区间和

前缀和

### 3 经典例题

- 比较两个有序队列的最大值，从大到小合并
    - 刷题初始化数组的方式`let mut ans = vec![0;n];`
    - 倒序添加数组元素 `for p in(0..n).rev()`

# 三、经典题目

[二分法](https://www.notion.so/5fcc07aae5104b6a8a8d315ba6036ccf?pvs=21)

[移除元素](https://www.notion.so/b163819dc9544486a8f6c7c535bf28ae?pvs=21)

## 参考
- https://www.programmercarl.com/%E6%95%B0%E7%BB%84%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html