- Rust 还有两种浮_点数_的原始类型，它们是带小数点的数字。Rust 的浮点类型是 `f32` 和 `f64`，它们的大小分别为 32 位和 64 位。默认类型为 `f64` 因为在现代 CPU 上，它的速度与 `f32` 大致相同，但能够实现更高的精度。所有浮点类型都带符号。
```rust
fn main() {
    let x = 2.0; // f64

    let y: f32 = 3.0; // f32
}
```
浮点数根据 IEEE-754 标准表示。