```
 #[command(subcommand)]
    command: Option<Commands>,
```
枚举
```
#[derive(Subcommand)]

enum Commands {

    /// does testing things

    Test {

        /// lists test values

        #[arg(short, long)]

        list: bool,

    },

}
```
- 可选加Option

[参数定义模板思考模式](参数定义模板思考模式.md)