### 实例
#### 找到切片len的元素有就copied没有就返回一个’/‘
```
   let next_byte_or_slash = |bytes: &[u8], len: usize, is_dir: bool| {

                bytes

                    .get(len)

                    .copied()

                    .or(if is_dir { Some(b'/') } else { None })

            };
```