```rust
#[test]

fn test2() {

    let mut node = Node {

        val: 1,

        next: None,

    };

  

    let next_ref = node.next.as_mut(); // 可变借用 node.next

    let x= next_ref.is_none();
    node.next = None;

    print!("{x}");

}
```
结果：
```
   --> src/second.rs:269:5
    |
268 |     let next_ref = node.next.as_mut(); // 可变借用 node.next
    |                    --------- `node.next` is borrowed here
269 |     node.next = None;                     // ❌ 同时修改 node → 编译器报错
    |     ^^^^^^^^^ `node.next` is assigned to here but it was already borrowed
270 |     let x= next_ref.is_none();
    |            -------- borrow later used here
```