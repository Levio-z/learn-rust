```rust
                    let output = results

                        .into_iter()

                        .collect::<Result<Vec<_>, _>>()?

                        .into_iter()

                        .collect::<Vec<_>>()

                        .join(" ");
```