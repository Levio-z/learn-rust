```rust
let mut file = File::create(path)?;
for chunk in data.chunks(1024) {
    file.write_all(chunk)?;
}
```
}
这里可以逐块写入，比如每次 1 KB
优点：
可以写入非常大的文件而不占用太多内存
可以在写入过程中处理数据（压缩、加密等）
Git 对象存储通常很小（几十到几百字节），所以分块写入没有必要，但在处理大文件时非常常用