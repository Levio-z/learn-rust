`&raw const STACK` 是 **Rust 原始裸指针（raw pointer）语法**的一种写法。
- `&`：取地址运算符。
- `raw`：告诉编译器这是要生成一个 **裸指针**，而不是普通的引用。
- `const`：表示生成的裸指针是 `*const T` 类型，即 **不可变裸指针**。
- `STACK`：目标对象（这里是一个 `static mut STACK: [u8; N]` 数组）。