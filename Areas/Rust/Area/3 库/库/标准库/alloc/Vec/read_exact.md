buf.read_exact(&mut ret[..])

- 只保证 **底层缓冲区至少能容纳 `len() + size` 个元素**
- 不会向 `Vec` 中添加元素
- 所以 `len()` 保持原值（例如 0）
- 只是让后续插入操作不触发重新分配，提高性能

|方法|特性|内存策略|使用场景|
|---|---|---|---|
|`reserve`|保证最少容量|可能多分配以减少频繁扩容|预计后续会继续插入元素|
|`reserve_exact`|保证最少容量|尽量精确分配，不做多余分配|内存敏感或只需一次性扩容|
